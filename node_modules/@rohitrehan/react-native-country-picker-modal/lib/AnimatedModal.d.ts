import * as React from 'react';
interface Props {
    visible?: boolean;
    children: React.ReactNode;
}
declare const AnimatedModal: ({ children, visible }: Props) => React.JSX.Element;
export default AnimatedModal;
