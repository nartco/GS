import * as React from 'react';
import { SafeAreaView, StyleSheet, Platform } from 'react-native';
import AnimatedModal from './AnimatedModal';
import { Modal } from './Modal';
import useTheme from './CountryTheme';
import { CountryModalContext } from './CountryModalProvider';
const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
});
const CountryModal = ({ children, withModal, disableNativeModal, ...props }) => {
    const { backgroundColor } = useTheme();
    const { teleport } = React.useContext(CountryModalContext);
    const content = React.useMemo(() => (<SafeAreaView style={[styles.container, { backgroundColor }]}>
        {children}
      </SafeAreaView>), [backgroundColor, children]);
    React.useEffect(() => {
        if (disableNativeModal ?? false) {
            teleport(<AnimatedModal {...props}>{content}</AnimatedModal>);
        }
    }, [disableNativeModal, content, props, teleport]);
    if (withModal ?? true) {
        if (Platform.OS === 'web') {
            return <Modal {...props}>{content}</Modal>;
        }
        if (disableNativeModal ?? false) {
            return null;
        }
        return <Modal {...props}>{content}</Modal>;
    }
    return content;
};
export default CountryModal;
//# sourceMappingURL=CountryModal.js.map