const FILENAME_REGEX = /^retry-([0-9]+)-.+\.json$/;
function timestampFromFilename(filename) {
    const match = FILENAME_REGEX.exec(filename);
    if (match) {
        return Number(match[1]);
    }
}

export { timestampFromFilename as default };
