import type { Delivery, RetryQueue, TracePayload } from '@bugsnag/core-performance';
import type Directory from './directory';
export default class FileBasedRetryQueue implements RetryQueue {
    private readonly delivery;
    private readonly directory;
    private requestQueue;
    constructor(delivery: Delivery, directory: Directory);
    add(payload: TracePayload, time: number): Promise<void>;
    flush(): Promise<void>;
    private flushFile;
    private getPayloadFromFile;
}
//# sourceMappingURL=file-based.d.ts.map