import type { Clock, InternalConfiguration, Plugin, SpanFactory } from '@bugsnag/core-performance';
import type { AppRegistry, WrapperComponentProvider } from 'react-native';
import type { ReactNativeConfiguration } from '../config';
export declare const isWrapperComponentProvider: (value: unknown) => value is WrapperComponentProvider | null;
export declare class AppStartPlugin implements Plugin<ReactNativeConfiguration> {
    private readonly appStartTime;
    private readonly spanFactory;
    private readonly clock;
    private readonly appRegistry;
    constructor(appStartTime: number, spanFactory: SpanFactory<ReactNativeConfiguration>, clock: Clock, appRegistry: typeof AppRegistry);
    configure(configuration: InternalConfiguration<ReactNativeConfiguration>): void;
}
//# sourceMappingURL=app-start-plugin.d.ts.map