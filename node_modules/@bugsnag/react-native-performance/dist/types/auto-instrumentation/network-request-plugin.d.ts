import type { InternalConfiguration, Plugin, SpanContextStorage, SpanFactory } from '@bugsnag/core-performance';
import type { NetworkRequestInfo, RequestTracker } from '@bugsnag/request-tracker-performance';
import type { ReactNativeConfiguration } from '../config';
export interface ReactNativeNetworkRequestInfo extends NetworkRequestInfo {
    readonly type: 'xmlhttprequest';
}
export declare class NetworkRequestPlugin implements Plugin<ReactNativeConfiguration> {
    private spanFactory;
    private readonly spanContextStorage;
    private xhrTracker;
    private ignoredUrls;
    private tracePropagationUrls;
    private networkRequestCallback;
    private logger;
    constructor(spanFactory: SpanFactory<ReactNativeConfiguration>, spanContextStorage: SpanContextStorage, xhrTracker: RequestTracker);
    configure(configuration: InternalConfiguration<ReactNativeConfiguration>): void;
    private trackRequest;
    private shouldTrackRequest;
    private shouldPropagateTraceContext;
    private getExtraRequestHeaders;
}
//# sourceMappingURL=network-request-plugin.d.ts.map